> [!IMPORTANT]
> Avoid rewriting published git history — never force push, force pull, rebase,
> amend, or squash commits that are already pushed, as it rewrites history and
> collaborators can lose project history.
>
> Keep the tracked branch in a working state at all times — every push should
> build cleanly.